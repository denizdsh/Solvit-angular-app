import { ValidateSameValueDirective } from './validate-same-value.directive';

describe('ValidateSameValueDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateSameValueDirective();
    expect(directive).toBeTruthy();
  });
});
