import { ValidateMatchPatternDirective } from './validate-match-pattern.directive';

describe('ValidateMatchPatternDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateMatchPatternDirective();
    expect(directive).toBeTruthy();
  });
});
